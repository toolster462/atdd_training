Given(/^the user is viewing the details of the product$/) do
  pending
end

When(/^the user adds a product to the cart$/) do
  pending
end

Then(/^the product is in the cart$/) do
  pending
end

Given(/^\?$/) do
  pending
end

When(/^When the user adds multiple different products to the cart$/) do
  pending
end

Then(/^each of the products is listed in the cart$/) do
  pending
end

When(/^When the user adds multiple of the same products to the cart$/) do
  pending
end

Then(/^the cart will list that item with the appropriate quantity$/) do
  pending
end

Given(/^there's an item in the cart$/) do
  pending
end

When(/^When the user changes quantities of items in the cart$/) do
  pending
end

Then(/^the quantity of items in the cart will be what the user changed it to$/) do
  pending
end

When(/^the user deletes an item from the cart$/) do
  pending
end

Then(/^the item is no longer in the cart$/) do
  pending
end

Given(/^there's items in the cart$/) do
  pending
end

Then(/^the subtotal is the sum of the prices of items of the cart$/) do
  pending
end

Given(/^the user isn't signed in$/) do
  pending
end

When(/^the user checks out$/) do
  pending
end

Then(/^the user is prompted to sign in$/) do
  pending
end



